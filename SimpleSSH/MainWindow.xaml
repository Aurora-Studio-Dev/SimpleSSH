<Window x:Class="SimpleSSH.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        mc:Ignorable="d"
        Title="SimpleSSH"
        ui:ThemeManager.IsThemeAware="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:TitleBar.ExtendViewIntoTitleBar="True"
        ui:TitleBar.IsIconVisible="False"
        ui:TitleBar.Height="30"
        Height="650"
        Width="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0"
            Height="50"
            Margin="10,0,0,0">
            <TextBlock
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Text="SimpleSSH"
                FontWeight="Bold" />
            <TextBox
                Height="35"
                MinWidth="450"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                VerticalContentAlignment="Center"
                HorizontalContentAlignment="Left"
                ui:ControlHelper.PlaceholderText="搜索一个服务器吧!" />
        </Grid>
        <ui:NavigationView
            ItemInvoked="NavigationTriggered"
            PaneDisplayMode="Top"
            IsBackButtonVisible="Collapsed"
            Grid.Row="1">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="首页"
                    Tag="SimpleSSH.Pages.HomePage"
                    IsSelected="True">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon
                            Icon="{x:Static ui:SegoeFluentIcons.Home}"
                            FontSize="16" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem
                    Content="SSH"
                    Tag="SimpleSSH.Pages.SSHPage"
                    IsSelected="True">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon
                            Icon="{x:Static ui:SegoeFluentIcons.Comment}"
                            FontSize="16" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem
                    Content="设置"
                    Tag="SimpleSSH.Pages.SettingsPage">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon
                            Icon="{x:Static ui:SegoeFluentIcons.Settings}"
                            FontSize="16" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>
            <ui:Frame x:Name="CurrentPage"
                      Margin="15" />
        </ui:NavigationView>
    </Grid>
</Window>